import { ThousandsSuffixPipe } from './thousands-suffix.pipe';

describe('ThousandsSuffixPipe', () => {
  it('create an instance', () => {
    const pipe = new ThousandsSuffixPipe();
    expect(pipe).toBeTruthy();
  });
});
